#!/bin/bash
if [ -n "$NVIM_LISTEN_ADDRESS" ]; then
    vimf=$(dirname "$(readlink -f "$0")")/../vim/remote_funcs.vim

    env -u NVIM_LISTEN_ADDRESS nvim --headless -u NONE \
    -c "source $vimf" -c "RemoteStart $NVIM_LISTEN_ADDRESS" \
    -c "RemoteMan $@" -c "quit"
else
    /usr/bin/man "$@"
fi
